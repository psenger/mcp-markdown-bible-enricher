# Agent OS Standards Index

books:
  book-map-conventions:
    description: BOOK_MAP key casing rules, abbrev source, and alternative name pattern
  single-chapter-books:
    description: Why singleChapter:true books use [[Abbrev#vN]] not [[Abbrev-01#vN]]

config:
  obsidian-links-opt-out:
    description: INCLUDE_OBSIDIAN_LINKS opt-out design; only 'false' disables it
  startup-config-loading:
    description: Config loaded once at module init; env changes require server restart

enrichment:
  double-link-prevention:
    description: Regex lookbehind/lookahead guards for idempotency and link preservation
  frontmatter-awareness:
    description: enrichMarkdown() strips YAML frontmatter before enrichment and reattaches it unchanged
  implicit-chapter-inheritance:
    description: parseChapterVerse() chapter propagation across comma-separated verse groups
  three-pass-ordering:
    description: Strict pass order in enrichMarkdown() and why it must not change

global:
  coupling-cohesion:
    description: Low coupling and high cohesion; dependency types, cohesion levels, stable and acyclic dependencies
  delegation:
    description: Forwarding calls to injected collaborators; delegation vs inheritance; enables Decorator, Proxy, Facade
  dry:
    description: Single authoritative representation of knowledge; AHA principle; DRY trade-offs in test code
  gang-of-four:
    description: 23 GoF patterns — when to use Creational, Structural, and Behavioral patterns; patterns to avoid
  hexagonal-architecture:
    description: Ports-and-adapters architecture; core/driving/driven zones; mock at boundaries; orthogonal design
  object-interaction:
    description: Law of Demeter and Tell-Don't-Ask; avoid chained calls; delegate decisions to the object that owns the data
  progressive-enhancement:
    description: Core path always works; enhancements activate conditionally; graceful degradation for non-critical failures
  simplicity:
    description: YAGNI, KISS, and Kent Beck's four rules of simple design
  solid:
    description: SRP, OCP, LSP, ISP, DIP with examples and relation to hexagonal architecture
  tdd-workflow:
    description: Red-green-refactor cycle, vertical slices, spec-first development, mocking rules, commit discipline
  value-objects:
    description: Immutable domain types that eliminate primitive obsession and encapsulate validation invariants

testing:
  config-module-cache-limitation:
    description: Why process.env changes in Jest beforeEach don't affect enrichment config

